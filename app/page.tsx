import { Experience } from "@/components/experience";

/**
 * Page unique du site (one page).
 * Toute l'expérience interactive vit dans <Experience />.
 */
export default function Home() {
  return <Experience />;
}
